<h1 class="text-center mt-4">Articles Listing</h1>

<div class="flickr_pagination">
    <%= will_paginate @articles, :cotainer => false %>
</div>

<%= render partial: 'article', locals: { articles: @articles } %>

<div class="flickr_pagination mb-4">
    <%= will_paginate @articles, :cotainer => false %>
</div>
